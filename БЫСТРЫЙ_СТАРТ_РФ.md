# ⚡ Быстрый старт для России

## 🎯 Самый простой вариант: Timeweb (400₽/мес)

### За 30 минут от нуля до работающего сайта

---

## Шаг 1: Регистрация (5 минут)

1. Откройте: https://timeweb.com/ru/services/vds
2. Нажмите **"Заказать"**
3. Выберите:
   - Тариф: **Medium** (400₽/мес)
   - ОС: **Ubuntu 22.04**
   - Локация: **Москва** или **Санкт-Петербург**
4. Зарегистрируйтесь (email + телефон)
5. Оплатите картой (принимают МИР, российские Visa/MC, СБП)

✅ Получите письмо с данными для доступа (IP, пароль root)

---

## Шаг 2: Подключение к серверу (2 минуты)

### На Mac/Linux:
```bash
ssh root@IP-адрес-из-письма
# Введите пароль из письма
# При первом подключении напишите: yes
```

### На Windows:
1. Скачайте PuTTY: https://www.putty.org/
2. Введите IP адрес
3. Login: `root`, пароль из письма

---

## Шаг 3: Установка приложения (15 минут)

Скопируйте команды по очереди и вставляйте в терминал:

### 3.1 Обновление системы
```bash
apt update && apt upgrade -y
```

### 3.2 Установка необходимого ПО
```bash
apt install -y python3 python3-pip python3-venv nginx git certbot python3-certbot-nginx
```

### 3.3 Создание пользователя
```bash
adduser docxapp
# Введите пароль (придумайте любой)
# Остальные вопросы - просто нажимайте Enter

usermod -aG sudo docxapp
su - docxapp
```

### 3.4 Загрузка приложения

**Вариант А: Если код на GitHub**
```bash
git clone https://github.com/ваш-username/docx-filler.git docx-template-filler
cd docx-template-filler
```

**Вариант Б: Загрузка файлов с компьютера**

На вашем Mac:
```bash
cd /Users/m.melnikov/projects/docx-changer
tar -czf docx-app.tar.gz --exclude='venv' --exclude='__pycache__' .
scp docx-app.tar.gz root@IP-сервера:/tmp/
```

На сервере:
```bash
mkdir -p ~/docx-template-filler
cd ~/docx-template-filler
tar -xzf /tmp/docx-app.tar.gz
```

### 3.5 Установка Python окружения
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
exit  # Вернетесь к root
```

### 3.6 Настройка автозапуска
```bash
cat > /etc/systemd/system/docxapp.service << 'EOF'
[Unit]
Description=DOCX Template Filler
After=network.target

[Service]
User=docxapp
Group=www-data
WorkingDirectory=/home/docxapp/docx-template-filler
Environment="PATH=/home/docxapp/docx-template-filler/venv/bin"
ExecStart=/home/docxapp/docx-template-filler/venv/bin/gunicorn --bind 127.0.0.1:8000 --workers 2 --timeout 120 app:app
Restart=always

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable docxapp
systemctl start docxapp
systemctl status docxapp  # Должно быть: active (running)
```

### 3.7 Настройка Nginx

**Замените `ВАШ_IP` на реальный IP адрес сервера!**

```bash
cat > /etc/nginx/sites-available/docxapp << 'EOF'
server {
    listen 80;
    server_name ВАШ_IP;
    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static {
        alias /home/docxapp/docx-template-filler/static;
    }
}
EOF

ln -s /etc/nginx/sites-available/docxapp /etc/nginx/sites-enabled/
systemctl restart nginx
```

---

## Шаг 4: Настройка Firewall в Timeweb (2 минуты)

1. Зайдите в панель Timeweb: https://timeweb.cloud
2. Выберите ваш сервер
3. Вкладка **"Firewall"**
4. Добавьте правила:
   - **HTTP**: Порт 80, Протокол TCP, Источник: Любой
   - **HTTPS**: Порт 443, Протокол TCP, Источник: Любой
   - **SSH**: Порт 22, Протокол TCP, Источник: Только мой IP (для безопасности)

---

## Шаг 5: Проверка (1 минута)

Откройте в браузере: `http://IP-адрес-вашего-сервера`

**Должно открыться приложение DOCX Template Filler!** 🎉

---

## 🌐 Подключение домена (опционально, +10 минут)

### Покупка домена
- **REG.RU** - от 99₽/год: https://www.reg.ru/domain/new/
- **2domains.ru** - от 149₽/год
- Выберите доступное имя, оплатите

### Настройка DNS
1. В панели регистратора найдите "Управление DNS"
2. Добавьте A-запись:
   ```
   Имя: @
   Тип: A
   Значение: IP-адрес-вашего-сервера
   TTL: 3600
   ```
3. Опционально для www:
   ```
   Имя: www
   Тип: A
   Значение: IP-адрес-вашего-сервера
   ```

### Обновление конфигурации Nginx

```bash
# Измените server_name в конфиге
nano /etc/nginx/sites-available/docxapp

# Найдите строку: server_name ВАШ_IP;
# Замените на: server_name ваш-домен.ru www.ваш-домен.ru;

# Сохраните: Ctrl+O, Enter, Ctrl+X

systemctl restart nginx
```

### Установка SSL (HTTPS)

```bash
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Введите email
# Согласитесь с условиями: Y
# Получать новости: N (или Y, как хотите)
```

**Готово!** Теперь ваш сайт работает по HTTPS! 🔒

Откройте: `https://ваш-домен.ru`

---

## 📝 Полезные команды

### Проверка статуса
```bash
systemctl status docxapp  # Статус приложения
systemctl status nginx    # Статус веб-сервера
```

### Просмотр логов
```bash
journalctl -u docxapp -f  # Логи приложения (нажмите Ctrl+C для выхода)
tail -f /var/log/nginx/error.log  # Логи Nginx
```

### Перезапуск
```bash
systemctl restart docxapp  # Перезапуск приложения
systemctl restart nginx    # Перезапуск веб-сервера
```

### Остановка
```bash
systemctl stop docxapp
systemctl stop nginx
```

---

## 🔄 Обновление приложения

Когда внесли изменения в код:

```bash
su - docxapp
cd docx-template-filler
git pull  # Если используете GitHub
source venv/bin/activate
pip install -r requirements.txt
exit

systemctl restart docxapp
```

---

## ⚠️ Если что-то не работает

### Приложение не открывается в браузере

**Проверьте статус:**
```bash
systemctl status docxapp
systemctl status nginx
```

**Если docxapp не работает:**
```bash
journalctl -u docxapp -n 50  # Последние 50 строк логов
```

**Если nginx не работает:**
```bash
nginx -t  # Проверка конфигурации
tail -f /var/log/nginx/error.log
```

### Забыли пароль от сервера

1. Зайдите в панель Timeweb
2. Выберите сервер
3. Кнопка "Сбросить пароль root"
4. Новый пароль придет на email

### Порты закрыты

Проверьте Firewall в панели Timeweb - должны быть открыты порты 80 и 443.

---

## 💰 Стоимость

**Минимальная:**
- Timeweb VPS Medium: 400₽/мес
- **Итого: 400₽/мес** (работает по IP)

**С доменом:**
- Timeweb VPS Medium: 400₽/мес
- Домен .ru: 99₽/год (~8₽/мес)
- **Итого: ~408₽/мес**

**С автобэкапами:**
- Timeweb VPS Medium: 400₽/мес
- Автобэкапы: 50₽/мес
- Домен .ru: ~8₽/мес
- **Итого: ~458₽/мес**

---

## 📞 Нужна помощь?

**Поддержка Timeweb:**
- Телефон: 8 800 775 51 59 (бесплатно)
- Email: support@timeweb.com
- Чат: в панели управления

**Работают 24/7, отвечают быстро!**

---

## ✅ Контрольный список

- [ ] Зарегистрировался на Timeweb
- [ ] Оплатил VPS Medium
- [ ] Получил IP и пароль
- [ ] Подключился по SSH
- [ ] Установил все пакеты
- [ ] Создал пользователя docxapp
- [ ] Загрузил код приложения
- [ ] Настроил systemd сервис
- [ ] Настроил Nginx
- [ ] Открыл порты в Firewall
- [ ] Проверил работу в браузере
- [ ] (Опционально) Купил домен
- [ ] (Опционально) Настроил DNS
- [ ] (Опционально) Установил SSL

---

**Готово!** Приложение работает и доступно из интернета! 🚀

**Время на всё:** 30-40 минут
**Стоимость:** 400₽/месяц
**Сложность:** Средняя (следуй инструкциям)

Поделитесь ссылкой с коллегами и пользуйтесь!

# 🚀 Варианты развертывания приложения

## Обзор вариантов

| Вариант | Сложность | Стоимость/мес | Плюсы | Минусы |
|---------|-----------|---------------|-------|--------|
| **Railway** | ⭐ Легко | $5-10 | Автоматическое развертывание, бесплатный SSL | Ограничения free tier |
| **Render** | ⭐ Легко | $7-25 | Простая настройка, автомасштабирование | Медленный холодный старт |
| **VPS (Hetzner)** | ⭐⭐⭐ Средне | €4-10 | Полный контроль, дешево | Нужна ручная настройка |
| **VPS (DigitalOcean)** | ⭐⭐⭐ Средне | $6-12 | Хорошая документация | Дороже Hetzner |
| **Timeweb/Beget** | ⭐⭐ Легко | 200-500₽ | Русская поддержка, простой интерфейс | Меньше гибкости |

## 🎯 Рекомендация

**Для начала:** Railway или Render (проще всего)
**Для экономии:** Hetzner VPS (самый дешевый)
**Для России:** Timeweb (русская поддержка)

---

## Вариант 1: Railway (САМЫЙ ПРОСТОЙ) ⭐

### Почему Railway?
- ✅ Развертывание за 5 минут
- ✅ Автоматический SSL сертификат
- ✅ Бесплатный поддомен (например, `docx-filler.up.railway.app`)
- ✅ Автоматические обновления из Git
- ✅ 500 часов бесплатно/месяц (потом $5/мес)

### Пошаговая инструкция

#### 1. Подготовка проекта

Создайте файлы для Railway:

**`Procfile`** (команда запуска):
```
web: gunicorn app:app
```

**`runtime.txt`** (версия Python):
```
python-3.9.18
```

**Обновите `requirements.txt`**:
```bash
# Добавьте gunicorn для production
echo "gunicorn==21.2.0" >> requirements.txt
```

#### 2. Инициализация Git (если еще не сделано)

```bash
cd /Users/m.melnikov/projects/docx-changer
git init
git add .
git commit -m "Initial commit"
```

#### 3. Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Создайте новый репозиторий (например, `docx-template-filler`)
3. Не добавляйте README, .gitignore (у вас уже есть)

```bash
git remote add origin https://github.com/ваш-username/docx-template-filler.git
git branch -M main
git push -u origin main
```

#### 4. Развертывание на Railway

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий
5. Railway автоматически определит Flask приложение
6. Нажмите **"Deploy"**
7. Готово! Через 2-3 минуты получите ссылку

#### 5. Настройка переменных окружения (опционально)

В Railway → Settings → Variables добавьте:
```
FLASK_ENV=production
MAX_CONTENT_LENGTH=10485760
```

#### 6. Получение домена

**Вариант А (бесплатный):**
- Railway даст поддомен: `docx-filler.up.railway.app`
- SSL сертификат уже включен

**Вариант Б (свой домен):**
- Купите домен на https://reg.ru (~300₽/год)
- В Railway: Settings → Custom Domain → добавьте ваш домен
- В настройках домена добавьте CNAME запись

---

## Вариант 2: Render (ТОЖЕ ПРОСТОЙ) ⭐

### Почему Render?
- ✅ Бесплатный tier (с ограничениями)
- ✅ Автоматический SSL
- ✅ Простая настройка

### Инструкция

#### 1. Подготовка

Создайте файл **`render.yaml`**:
```yaml
services:
  - type: web
    name: docx-filler
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
```

#### 2. Развертывание

1. Зайдите на https://render.com
2. Войдите через GitHub
3. New → Web Service
4. Подключите репозиторий
5. Render автоматически найдет `render.yaml`
6. Нажмите "Create Web Service"
7. Готово!

**Бесплатный tier:**
- Приложение "засыпает" после 15 минут неактивности
- Холодный старт ~30 секунд
- 750 часов/месяц бесплатно

**Платный tier ($7/мес):**
- Без засыпания
- Быстрый отклик

---

## Вариант 3: VPS на Hetzner (ЭКОНОМНЫЙ) ⭐⭐⭐

### Почему Hetzner?
- ✅ Самый дешевый: от €3.79/мес (CX11)
- ✅ Европейские серверы (быстро для России)
- ✅ Полный контроль

### Характеристики сервера

**CX11 (€3.79/мес):**
- 1 vCPU
- 2 GB RAM
- 20 GB SSD
- 20 TB трафик
- Достаточно для вашего приложения

### Пошаговая инструкция

#### 1. Создание сервера

1. Зарегистрируйтесь на https://www.hetzner.com
2. Console → Projects → New Project → "docx-filler"
3. Add Server → Location: Нюрнберг (ближе к России)
4. Image: **Ubuntu 22.04**
5. Type: **CX11**
6. SSH Key: создайте или загрузите свой
7. Create & Buy Now

#### 2. Подключение к серверу

```bash
# Получите IP адрес из консоли Hetzner
ssh root@ваш-IP-адрес
```

#### 3. Установка необходимого ПО

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и зависимостей
apt install -y python3 python3-pip python3-venv nginx certbot python3-certbot-nginx

# Установка Git
apt install -y git

# Создание пользователя для приложения
adduser docxapp
usermod -aG sudo docxapp
su - docxapp
```

#### 4. Загрузка приложения

```bash
# Клонирование репозитория
git clone https://github.com/ваш-username/docx-template-filler.git
cd docx-template-filler

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
pip install gunicorn
```

#### 5. Создание systemd сервиса

```bash
sudo nano /etc/systemd/system/docxapp.service
```

Содержимое файла:
```ini
[Unit]
Description=DOCX Template Filler
After=network.target

[Service]
User=docxapp
Group=www-data
WorkingDirectory=/home/docxapp/docx-template-filler
Environment="PATH=/home/docxapp/docx-template-filler/venv/bin"
ExecStart=/home/docxapp/docx-template-filler/venv/bin/gunicorn --workers 2 --bind 127.0.0.1:8000 app:app

[Install]
WantedBy=multi-user.target
```

Запуск сервиса:
```bash
sudo systemctl start docxapp
sudo systemctl enable docxapp
sudo systemctl status docxapp
```

#### 6. Настройка Nginx

```bash
sudo nano /etc/nginx/sites-available/docxapp
```

Содержимое:
```nginx
server {
    listen 80;
    server_name ваш-домен.ru;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static {
        alias /home/docxapp/docx-template-filler/static;
        expires 30d;
    }
}
```

Активация конфига:
```bash
sudo ln -s /etc/nginx/sites-available/docxapp /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 7. Настройка домена

**Купите домен** (reg.ru, namecheap.com, cloudflare.com):

В DNS настройках домена добавьте A-запись:
```
Тип: A
Имя: @
Значение: IP-адрес-вашего-сервера
TTL: 3600
```

Опционально добавьте www:
```
Тип: A
Имя: www
Значение: IP-адрес-вашего-сервера
TTL: 3600
```

#### 8. Установка SSL сертификата (бесплатно)

```bash
sudo certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```

Certbot автоматически:
- Получит сертификат от Let's Encrypt
- Настроит автообновление
- Перенаправит HTTP → HTTPS

#### 9. Автоматические обновления

Создайте скрипт обновления:
```bash
nano ~/update.sh
```

Содержимое:
```bash
#!/bin/bash
cd /home/docxapp/docx-template-filler
git pull origin main
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart docxapp
```

Сделайте исполняемым:
```bash
chmod +x ~/update.sh
```

Теперь для обновления просто:
```bash
./update.sh
```

---

## Вариант 4: Российские хостинги (TIMEWEB/BEGET)

### Timeweb (если важна русская поддержка)

**Преимущества:**
- ✅ Русская поддержка 24/7
- ✅ Серверы в России (быстрее)
- ✅ Простая панель управления
- ✅ От 200₽/мес

**Тарифы:**
- Start: 200₽/мес (1 CPU, 512MB RAM)
- Medium: 400₽/мес (2 CPU, 2GB RAM) - рекомендуется

### Инструкция для Timeweb

1. Зарегистрируйтесь на https://timeweb.com
2. Выберите VDS → тариф Medium
3. ОС: Ubuntu 22.04
4. Следуйте инструкциям из "Вариант 3: Hetzner"
5. Timeweb предоставит IP и доступ

---

## Вариант 5: Docker (УНИВЕРСАЛЬНЫЙ)

Если хотите развернуть где угодно с помощью Docker.

### Создание Dockerfile

Создайте файл **`Dockerfile`**:
```dockerfile
FROM python:3.9-slim

WORKDIR /app

# Установка системных зависимостей для python-docx
RUN apt-get update && apt-get install -y \
    libxml2-dev \
    libxslt-dev \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
RUN pip install gunicorn

COPY . .

# Создание директорий
RUN mkdir -p uploads output docx_templates examples

EXPOSE 5000

CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "2", "app:app"]
```

### Создание docker-compose.yml

```yaml
version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./output:/app/output
      - ./docx_templates:/app/docx_templates
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
```

### Запуск

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

---

## 🎯 Рекомендуемый план действий

### Для быстрого старта (1 день):
1. ✅ Создайте репозиторий на GitHub
2. ✅ Разверните на Railway (бесплатно)
3. ✅ Используйте бесплатный поддомен
4. ✅ Протестируйте

### Для production (1 неделя):
1. ✅ Купите домен на reg.ru (~300₽/год)
2. ✅ Арендуйте VPS на Hetzner (€3.79/мес)
3. ✅ Настройте по инструкции "Вариант 3"
4. ✅ Установите SSL сертификат
5. ✅ Настройте мониторинг

### Итоговая стоимость:

**Минимальная (Railway):**
- Домен: 300₽/год (~25₽/мес)
- Хостинг: $5/мес (~450₽)
- **Итого: ~475₽/мес**

**Оптимальная (Hetzner VPS):**
- Домен: 300₽/год (~25₽/мес)
- VPS: €3.79/мес (~380₽)
- **Итого: ~405₽/мес**

**Максимальная (отказоустойчивая):**
- Домен: 300₽/год
- VPS: €10/мес (~1000₽)
- Резервный сервер: €5/мес (~500₽)
- **Итого: ~1525₽/мес**

---

## 📝 Контрольный список перед запуском

- [ ] Приложение протестировано локально
- [ ] Создан Git репозиторий
- [ ] Добавлен .gitignore (секреты не в Git!)
- [ ] Куплен домен (опционально)
- [ ] Выбрана платформа развертывания
- [ ] Настроен SSL сертификат
- [ ] Проверена работа на production
- [ ] Настроено автообновление (для VPS)
- [ ] Настроен мониторинг (Uptime Robot)
- [ ] Создана резервная копия

## 🔒 Безопасность

⚠️ **Важные моменты:**

1. **Ограничение размера файлов** - уже настроено (10 MB)
2. **Валидация файлов** - только .docx
3. **Очистка временных файлов** - уже реализована
4. **HTTPS обязательно** - используйте SSL
5. **Регулярные обновления** - обновляйте зависимости

---

## 💡 Следующие шаги

1. Выберите вариант развертывания
2. Следуйте инструкции
3. Протестируйте приложение
4. Поделитесь ссылкой с пользователями!

Если нужна помощь с конкретным вариантом - дайте знать!

# Возможности приложения

## 🎯 Основные функции

### 1. Автоматическое извлечение полей (NEW in v2.0)

При загрузке DOCX шаблона приложение:
- Сканирует документ и находит все Jinja2 теги
- Определяет тип каждого поля:
  - **Simple** - простые текстовые переменные `{{name}}`
  - **Boolean** - условные переменные `{% if has_discount %}`
  - **Array** - массивы данных `{% for item in items %}`
- Извлекает структуру объектов внутри массивов

**Пример:**
```
Шаблон содержит: {{client_name}}, {{date}}, {% for item in items %}{{item.name}}{% endfor %}
↓
Извлекается:
- client_name (simple)
- date (simple)
- items (array с полями: name)
```

### 2. Динамическая генерация формы (NEW in v2.0)

На основе найденных полей автоматически создается удобная форма:

**Простые поля:**
```
{{company}} → [Текстовое поле: "Введите company"]
```

**Boolean поля:**
```
{% if has_discount %} → [✓ has_discount]
```

**Массивы:**
```
{% for item in items %}
  {{item.name}} - {{item.price}}
{% endfor %}

↓ Превращается в:

┌─ items (массив) ────────────────┐
│ Элемент 1:                       │
│   name:  [________]              │
│   price: [________]              │
│   [Удалить]                      │
├──────────────────────────────────┤
│ Элемент 2:                       │
│   name:  [________]              │
│   price: [________]              │
│   [Удалить]                      │
└──────────────────────────────────┘
[+ Добавить items]
```

### 3. Переключение режимов

**Режим "Форма"** (по умолчанию):
- Удобно для простых случаев
- Не нужно знать JSON
- Визуальное заполнение полей
- Автоматическая валидация

**Режим "JSON"**:
- Для опытных пользователей
- Полный контроль над структурой данных
- Возможность копировать/вставлять готовый JSON
- Real-time валидация синтаксиса

### 4. Работа с массивами

Массивы в форме поддерживают:
- ➕ **Добавление элементов** - кнопка "+ Добавить"
- ➖ **Удаление элементов** - кнопка "Удалить" для каждого элемента
- 📝 **Автоматическая нумерация** - "Элемент 1", "Элемент 2", и т.д.
- 🔄 **Динамическое обновление** - добавляйте сколько угодно элементов

### 5. Умная сборка JSON

При нажатии "Сгенерировать документ" в режиме формы:
1. Собираются все значения из полей
2. Автоматически создается корректный JSON
3. Типы данных определяются автоматически:
   - Текстовые поля → строки
   - Чекбоксы → true/false
   - Массивы → массивы объектов
4. JSON отправляется на сервер

**Вы не видите JSON, но он генерируется правильно!**

## 🎨 Пользовательский интерфейс

### Drag & Drop
- Перетащите файл в зону загрузки
- Или кликните для выбора через диалог
- Поддерживаются только .docx файлы

### Визуальная обратная связь
- ✅ Уведомления об успешной загрузке
- ⚠️ Валидация в реальном времени
- 🔄 Индикатор процесса генерации
- 📊 Счетчик найденных полей

### Адаптивный дизайн
- Работает на десктопе и планшетах
- Bootstrap 5 для современного UI
- Интуитивно понятный интерфейс

## 🔧 Технические возможности

### Поддержка Jinja2
Все стандартные возможности Jinja2:
- Переменные: `{{var}}`
- Фильтры: `{{text|upper}}`
- Циклы: `{% for item in list %}...{% endfor %}`
- Условия: `{% if condition %}...{% endif %}`
- Вложенные объекты: `{{user.address.city}}`

### Безопасность
- Ограничение размера файла (10 MB)
- Валидация расширения (.docx только)
- Безопасные имена файлов
- Автоматическая очистка временных файлов (через 1 час)
- Изоляция пользовательских данных

### Производительность
- Парсинг шаблона: < 1 секунда
- Генерация документа: < 1 секунда
- Поддержка больших документов (до 10 MB)
- Кэширование загруженных шаблонов в сессии

## 📋 Примеры использования

### Пример 1: Простой договор

**Шаблон:**
```
Договор №{{contract_number}} от {{date}}
Между {{company}} и {{client}}
Сумма: {{amount}} руб.
```

**Форма автоматически создаст:**
- contract_number: [____]
- date: [____]
- company: [____]
- client: [____]
- amount: [____]

### Пример 2: Договор со списком услуг

**Шаблон:**
```
Услуги:
{% for service in services %}
- {{service.name}}: {{service.price}} руб.
{% endfor %}
```

**Форма создаст:**
```
services (массив):
┌─ Элемент 1 ───────┐
│ name:  [_______]  │
│ price: [_______]  │
│ [Удалить]         │
└───────────────────┘
[+ Добавить services]
```

### Пример 3: Условная скидка

**Шаблон:**
```
Итого: {{total}} руб.
{% if has_discount %}
Скидка: {{discount}}%
Итого со скидкой: {{total_with_discount}} руб.
{% endif %}
```

**Форма создаст:**
- total: [____]
- [✓] has_discount
- discount: [____]
- total_with_discount: [____]

## 🚀 Преимущества

### Для обычных пользователей:
✅ Не нужно знать JSON
✅ Простой визуальный интерфейс
✅ Понятно, что нужно заполнить
✅ Невозможно сделать синтаксическую ошибку

### Для опытных пользователей:
✅ Режим JSON доступен всегда
✅ Можно копировать/вставлять готовые данные
✅ Полный контроль над структурой
✅ Возможность использовать сложные конструкции

### Для всех:
✅ Быстро - загрузил и заполнил
✅ Наглядно - видно все поля сразу
✅ Безопасно - автоматическая валидация
✅ Гибко - два режима работы

## 📝 Ограничения

- Максимальный размер файла: 10 MB
- Поддерживаются только .docx файлы (не .doc)
- Сложные Jinja2 конструкции могут не распознаваться автоматически
- Для сложных случаев рекомендуется режим JSON
- Временные файлы хранятся 1 час

## 💡 Советы по использованию

1. **Для простых документов** - используйте режим "Форма"
2. **Для повторяющихся документов** - сохраните JSON и используйте режим "JSON"
3. **При работе с массивами** - начните с 1 элемента, проверьте результат, затем добавляйте остальные
4. **Если форма не распознала поле** - переключитесь в режим JSON и добавьте вручную
5. **Для отладки** - переключайтесь между режимами, чтобы видеть сгенерированный JSON

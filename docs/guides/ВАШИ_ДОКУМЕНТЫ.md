# Инструкция по работе с вашими документами

## ✅ Готово!

Ваши документы успешно подготовлены и готовы к использованию в приложении.

## 📁 Что было сделано

### 1. Конвертация шаблонов

Ваши оригинальные документы с тегами `{переменная}` были конвертированы в формат Jinja2 `{{переменная}}`:

**Исходные файлы:**
- `ТА-турист турпродукт 27082025.docx  пр..docx`
- `для договора.docx`

**Сконвертированные шаблоны:**
- `docx_templates/ТА-турист турпродукт 27082025.docx пр..docx`
- `docx_templates/для договора.docx`

### 2. Нормализация имен переменных

Для совместимости с Jinja2 были выполнены замены:

| Было | Стало | Причина |
|------|-------|---------|
| `{Адрес-Заказчика}` | `{{Адрес_Заказчика}}` | Дефис → подчеркивание |
| `{Адрес электронной почты}` | `{{Адрес_электронной_почты}}` | Пробелы → подчеркивания |
| `{Дата_выдачи_ документа}` | `{{Дата_выдачи_документа}}` | Лишние пробелы удалены |
| `{ЦЕНА_рубли_ Прописью}` | `{{ЦЕНА_рубли_Прописью}}` | Лишние пробелы удалены |

**Почему это важно:**
- Jinja2 не поддерживает пробелы и дефисы в именах переменных
- Все имена должны состоять из букв, цифр и подчеркиваний

### 3. Созданы примеры данных

В директории `examples/` созданы файлы с примерами JSON данных:

- `examples/турист_договор_данные.json` - для первого документа
- `examples/для_договора_данные.json` - для второго документа

## 🚀 Как использовать

### Вариант 1: Через веб-интерфейс (рекомендуется)

1. **Запустите приложение:**
   ```bash
   cd /Users/m.melnikov/projects/docx-changer
   source venv/bin/activate
   python app.py
   ```

2. **Откройте браузер:** http://localhost:5000

3. **Загрузите шаблон:**
   - Перетащите файл из `docx_templates/` в форму
   - Например: `docx_templates/для договора.docx`

4. **Заполните данные:**
   - **Режим "Форма"** (проще): Приложение автоматически создаст поля для всех переменных
   - **Режим "JSON"** (для опытных): Скопируйте данные из `examples/для_договора_данные.json`

5. **Нажмите "Сгенерировать документ"**

6. **Скачайте готовый файл**

### Вариант 2: Через командную строку (для автоматизации)

```bash
source venv/bin/activate

curl -X POST http://127.0.0.1:5000/generate \
  -F "template=@docx_templates/для договора.docx" \
  -F "data=$(cat examples/для_договора_данные.json)" \
  -s | python3 -m json.tool
```

## 📝 Список всех переменных

### Для "ТА-турист турпродукт 27082025.docx":

```json
{
  "номер_договора": "001/2026",
  "дата_договора": "25.01.2026",
  "Фамилия_Имя_Отчество": "Иванов Иван Иванович",
  "Серия_номер": "1234 567890",
  "Дата_выдачи_документа": "10.01.2015",
  "кем_выдан": "ОВД ...",
  "Адрес_Заказчика": "г. Москва, ...",
  "Телефон": "+7 (900) ...",
  "Адрес_электронной_почты": "email@example.com",
  "ЦЕНА_рубли": "150000",
  "ЦЕНА_рубли_Прописью": "сто пятьдесят тысяч",
  "ЦЕНА_Уе": "1500",
  "ЦЕНА_Уе_Прописью": "одна тысяча пятьсот"
}
```

### Для "для договора.docx":

Все переменные как выше **плюс:**
```json
{
  "туроператор": "ООО \"Туристическая компания\"",
  "Полная_оплата_до": "01.02.2026"
}
```

## ⚠️ Важные замечания

### 1. Имена переменных изменились!

**Если у вас уже есть готовые JSON файлы с данными**, обновите имена переменных:
- `Адрес-Заказчика` → `Адрес_Заказчика`
- `Адрес электронной почты` → `Адрес_электронной_почты`
- И т.д. (см. таблицу выше)

### 2. Не редактируйте оригинальные файлы

Если нужно внести изменения в шаблон:
1. Редактируйте **оригинальные** файлы (с одинарными скобками)
2. Запустите конвертер снова:
   ```bash
   python convert_brackets_final.py
   ```

### 3. Используйте корректные имена

В Word шаблоне теперь используются переменные с подчеркиваниями:
- ✅ `{{Адрес_Заказчика}}`
- ❌ `{{Адрес-Заказчика}}`

## 🔧 Устранение проблем

### Ошибка: "variable is undefined"

**Причина:** Переменная есть в шаблоне, но отсутствует в JSON.

**Решение:** Добавьте эту переменную в ваш JSON файл.

### Ошибка: "expected token 'end of print statement'"

**Причина:** В имени переменной есть недопустимые символы (пробелы, дефисы).

**Решение:** Пересоздайте шаблон через `convert_brackets_final.py`

### Генерируется пустой документ

**Причина:** Переменные написаны с ошибками.

**Решение:**
1. Проверьте регистр букв (точное совпадение)
2. Убедитесь, что используются подчеркивания, а не дефисы или пробелы

## 📚 Дополнительная информация

- **Полная документация:** `README.md`
- **Быстрый старт:** `QUICKSTART.md`
- **Возможности приложения:** `FEATURES.md`
- **История изменений:** `CHANGELOG.md`

## ✨ Примеры сгенерированных документов

Уже сгенерированы тестовые документы в `output/`:
- `filled_20260125_173411_docx` - "для договора.docx"
- `filled_20260125_173449_-_27082025.docx_..docx` - "ТА-турист..."

Откройте их, чтобы убедиться, что все работает правильно!

---

**Готово к работе!** 🎉

Если возникнут вопросы, обращайтесь к документации или запускайте приложение и используйте режим "Форма" - он автоматически покажет все поля для заполнения.

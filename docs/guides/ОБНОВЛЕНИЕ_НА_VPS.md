# 🔄 Обновление приложения на VPS

## Быстрый способ (один скрипт)

Подключитесь к серверу и выполните:

```bash
ssh root@85.239.39.232

# Переключитесь на пользователя docxapp
su - docxapp

# Перейдите в директорию приложения
cd docx-template-filler

# Запустите скрипт обновления
bash scripts/update_vps.sh
```

**Готово!** Приложение обновлено и перезапущено! ✅

---

## Пошаговый способ (если нужен контроль)

### 1. Подключение к серверу

```bash
ssh root@85.239.39.232
# Пароль: uy@bquppW3?DFYY
```

### 2. Переключение на пользователя приложения

```bash
su - docxapp
```

### 3. Переход в директорию приложения

```bash
cd docx-template-filler
```

### 4. Получение обновлений

```bash
# Посмотреть что изменилось
git fetch origin
git log HEAD..origin/main --oneline

# Применить обновления
git pull origin main
```

### 5. Обновление зависимостей (если менялся requirements.txt)

```bash
source venv/bin/activate
pip install -r requirements.txt
deactivate
```

### 6. Перезапуск приложения

```bash
# Вернитесь к root
exit

# Перезапустите сервис
systemctl restart docxapp

# Проверьте статус
systemctl status docxapp
```

### 7. Проверка работы

```bash
# Проверьте что приложение отвечает
curl http://localhost:8000/health

# Проверьте логи (если есть проблемы)
journalctl -u docxapp -f
```

---

## Если что-то пошло не так

### Приложение не запускается

```bash
# Смотрим логи
journalctl -u docxapp -n 50

# Проверяем синтаксис Python
su - docxapp
cd docx-template-filler
source venv/bin/activate
python -c "import app"
```

### Конфликт при git pull

```bash
# Если есть локальные изменения
git stash
git pull origin main
git stash pop

# Или просто сбросить к последней версии
git reset --hard origin/main
```

### Nginx не проксирует запросы

```bash
# Перезапустите Nginx
systemctl restart nginx

# Проверьте конфигурацию
nginx -t

# Смотрите логи
tail -f /var/log/nginx/error.log
```

---

## Автоматизация (опционально)

### Настройка автообновления по расписанию

Создайте cron задачу для обновления каждую ночь:

```bash
crontab -e -u docxapp

# Добавьте строку (обновление в 3:00 каждую ночь):
0 3 * * * cd /home/docxapp/docx-template-filler && git pull origin main && sudo systemctl restart docxapp
```

---

## Проверка версии

Чтобы узнать какая версия сейчас на сервере:

```bash
cd /home/docxapp/docx-template-filler
git log -1 --oneline
```

Сравните с локальной версией:
```bash
# На вашем Mac
git log -1 --oneline
```

Должны совпадать после `git push` и `git pull`!

---

## Откат к предыдущей версии

Если обновление сломало приложение:

```bash
# Посмотрите историю коммитов
git log --oneline -10

# Откатитесь к предыдущему коммиту
git reset --hard HASH_КОММИТА

# Перезапустите
sudo systemctl restart docxapp
```

---

## Полезные команды

| Команда | Описание |
|---------|----------|
| `git status` | Текущее состояние репозитория |
| `git log --oneline -5` | Последние 5 коммитов |
| `git diff` | Показать изменения |
| `systemctl status docxapp` | Статус приложения |
| `journalctl -u docxapp -f` | Логи в реальном времени |
| `curl http://localhost:8000/` | Проверка работы |

---

**Готово!** Теперь обновление приложения занимает 30 секунд! 🚀

# 🇷🇺 Развертывание приложения в России

## ⚠️ Актуальная ситуация (январь 2026)

Многие западные сервисы:
- ❌ Railway, Render - могут быть недоступны, проблемы с оплатой
- ❌ AWS, Google Cloud - санкции, сложная оплата
- ⚠️ Hetzner - работает, но нужна зарубежная карта
- ✅ Российские хостинги - работают стабильно

---

## ✅ Работающие варианты для России

### Вариант 1: Timeweb (РЕКОМЕНДУЮ) ⭐⭐⭐

**Почему хороший выбор:**
- ✅ Российская компания, стабильно работает
- ✅ Оплата российскими картами (Мир, Visa/MC российских банков)
- ✅ Русская поддержка 24/7 (телефон, чат, почта)
- ✅ Датацентры в России (быстро)
- ✅ Простая панель управления
- ✅ Автоматический SSL

**Тарифы:**
- **Start** - 200₽/мес (512MB RAM) - хватит для начала
- **Medium** - 400₽/мес (2GB RAM) - рекомендуется ⭐
- **Advanced** - 800₽/мес (4GB RAM) - для нагруженных проектов

**Характеристики Medium:**
- 2 CPU cores
- 2 GB RAM
- 40 GB SSD
- Безлимитный трафик
- IPv4 адрес

#### Пошаговая инструкция

**1. Регистрация и создание сервера**

Зайдите на https://timeweb.com/ru/services/vds

1. Зарегистрируйтесь (email + телефон)
2. Выберите **VDS** → тариф **Medium** (400₽/мес)
3. Операционная система: **Ubuntu 22.04**
4. Дополнительно:
   - ✅ Firewall (бесплатно)
   - ✅ Автобэкапы (+50₽/мес - рекомендуется)
5. Оплатите (принимают карты Мир, СБП)
6. Дождитесь создания сервера (5-10 минут)

**2. Подключение к серверу**

Timeweb пришлет вам:
- IP адрес сервера
- Логин: `root`
- Пароль (в письме или в панели)

На Mac/Linux:
```bash
ssh root@ваш-IP-адрес
# Введите пароль из письма
```

На Windows:
- Скачайте PuTTY: https://www.putty.org/
- Введите IP адрес
- Логин: root, пароль из письма

**3. Автоматическая установка приложения**

После подключения к серверу выполните:

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка необходимых пакетов
apt install -y python3 python3-pip python3-venv nginx git certbot python3-certbot-nginx

# Создание пользователя
adduser docxapp
# Придумайте пароль
usermod -aG sudo docxapp

# Переключение на пользователя
su - docxapp

# Клонирование вашего кода
# Вариант А: если код на GitHub
git clone https://github.com/ваш-username/docx-filler.git docx-template-filler

# Вариант Б: загрузка файлов с компьютера (см. ниже)

cd docx-template-filler

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Тестовый запуск
python app.py
# Нажмите Ctrl+C для остановки
```

**4. Настройка автозапуска (systemd)**

Выйдите из пользователя docxapp:
```bash
exit  # вернетесь к root
```

Создайте сервис:
```bash
nano /etc/systemd/system/docxapp.service
```

Вставьте:
```ini
[Unit]
Description=DOCX Template Filler
After=network.target

[Service]
User=docxapp
Group=www-data
WorkingDirectory=/home/docxapp/docx-template-filler
Environment="PATH=/home/docxapp/docx-template-filler/venv/bin"
ExecStart=/home/docxapp/docx-template-filler/venv/bin/gunicorn --bind 127.0.0.1:8000 --workers 2 --timeout 120 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```

Сохраните: `Ctrl+O`, Enter, `Ctrl+X`

Запустите сервис:
```bash
systemctl daemon-reload
systemctl enable docxapp
systemctl start docxapp
systemctl status docxapp
```

**5. Настройка Nginx**

```bash
nano /etc/nginx/sites-available/docxapp
```

Вставьте:
```nginx
server {
    listen 80;
    server_name ваш-IP-адрес;
    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static {
        alias /home/docxapp/docx-template-filler/static;
    }
}
```

Активируйте:
```bash
ln -s /etc/nginx/sites-available/docxapp /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

**6. Настройка Firewall в панели Timeweb**

В панели управления Timeweb:
1. Перейдите в раздел "Firewall"
2. Добавьте правила:
   - Порт 80 (HTTP) - открыть
   - Порт 443 (HTTPS) - открыть
   - Порт 22 (SSH) - открыть (только ваш IP для безопасности)

**7. Проверка**

Откройте в браузере: `http://ваш-IP-адрес`

Должно открыться приложение!

**8. Подключение домена (опционально)**

**Купите домен на российском регистраторе:**
- **REG.RU** - от 99₽/год (.ru)
- **2domains.ru** - от 149₽/год
- **RU-CENTER** - от 199₽/год

**Настройка DNS:**
1. В панели управления доменом найдите "Управление DNS"
2. Добавьте A-запись:
   ```
   Тип: A
   Имя: @
   Значение: IP-адрес-вашего-сервера
   TTL: 3600
   ```
3. Опционально добавьте www:
   ```
   Тип: A
   Имя: www
   Значение: IP-адрес-вашего-сервера
   ```

Подождите 5-30 минут для обновления DNS.

**9. Установка SSL (HTTPS)**

После привязки домена:
```bash
# В настройках Nginx замените IP на домен
nano /etc/nginx/sites-available/docxapp
# Измените server_name на: server_name ваш-домен.ru www.ваш-домен.ru

# Перезапустите Nginx
systemctl restart nginx

# Установите SSL
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```

**Готово!** Теперь ваш сайт доступен по HTTPS! 🎉

---

### Вариант 2: Beget (Простой вариант) ⭐⭐

**Почему может подойти:**
- ✅ Очень простая панель управления
- ✅ Хороший для начинающих
- ✅ Стабильная работа
- ✅ Оплата российскими картами

**Минусы:**
- ⚠️ Дороже Timeweb
- ⚠️ Меньше гибкости

**Тарифы:**
- VPS-1: 390₽/мес (1GB RAM)
- VPS-2: 690₽/мес (2GB RAM) - рекомендуется

Сайт: https://beget.com/ru/vps

Настройка аналогична Timeweb.

---

### Вариант 3: RU-CENTER (Для корпоративных клиентов)

**Плюсы:**
- ✅ Крупная компания, высокая надежность
- ✅ Хорошая поддержка
- ✅ Регистратор доменов + хостинг

**Минусы:**
- ⚠️ Дороже конкурентов
- ⚠️ Сложнее интерфейс

**Тарифы:** от 700₽/мес

Сайт: https://hosting.nic.ru/

---

### Вариант 4: DigitalOcean через РФ карты (Если есть UnionPay)

**Актуально, если:**
- Есть карта UnionPay
- Нужен международный сервис
- Хотите развернуть за рубежом

**Как оплатить:**
1. Получите карту UnionPay в банке (Газпромбанк, Росбанк и др.)
2. Зарегистрируйтесь на https://www.digitalocean.com
3. Привяжите карту UnionPay

**Тарифы:**
- Basic: $6/мес (1GB RAM)
- Regular: $12/мес (2GB RAM)

Настройка как для Hetzner (см. DEPLOYMENT.md).

---

### Вариант 5: Yandex Cloud ⭐

**Плюсы:**
- ✅ Российский облачный провайдер
- ✅ Оплата российскими картами
- ✅ Хорошая документация на русском
- ✅ Бесплатные ₽4000 на старте (пробный период)

**Минусы:**
- ⚠️ Сложнее в настройке
- ⚠️ Дороже обычного VPS

**Тарифы:**
- 2 vCPU, 2GB RAM: ~600-800₽/мес
- Бесплатный trial на 60 дней

#### Быстрая настройка

**1. Регистрация**

https://cloud.yandex.ru/

1. Войдите через Яндекс ID
2. Активируйте пробный период (₽4000)
3. Привяжите карту (спишут 1₽ для проверки)

**2. Создание виртуальной машины**

```bash
# Установите Yandex Cloud CLI на вашем компьютере
curl https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash

# Инициализация
yc init

# Создание VM
yc compute instance create \
  --name docx-filler \
  --zone ru-central1-a \
  --network-interface subnet-name=default-ru-central1-a,nat-ip-version=ipv4 \
  --create-boot-disk image-folder-id=standard-images,image-family=ubuntu-2204-lts,size=20 \
  --memory 2GB \
  --cores 2 \
  --core-fraction 100 \
  --ssh-key ~/.ssh/id_rsa.pub

# Получите IP адрес
yc compute instance get docx-filler
```

**3. Подключение и настройка**

```bash
ssh ubuntu@публичный-IP

# Установка (как для Timeweb выше)
```

---

## 📊 Сравнение российских хостингов

| Провайдер | Цена/мес | Поддержка | Сложность | Рекомендация |
|-----------|----------|-----------|-----------|--------------|
| **Timeweb** | 400₽ | ⭐⭐⭐⭐⭐ Отлично | ⭐⭐ Средне | ✅ Лучший выбор |
| **Beget** | 690₽ | ⭐⭐⭐⭐ Хорошо | ⭐ Легко | ✅ Для новичков |
| **RU-CENTER** | 700₽ | ⭐⭐⭐⭐ Хорошо | ⭐⭐ Средне | ⚠️ Дороже |
| **Yandex Cloud** | 600-800₽ | ⭐⭐⭐ Средне | ⭐⭐⭐ Сложно | ⚠️ Для опытных |
| **REG.RU** | 500₽ | ⭐⭐⭐ Средне | ⭐⭐ Средне | ✅ Хороший вариант |

---

## 💡 Моя рекомендация для России

### Для новичков:
**Beget VPS-2** (690₽/мес)
- Самая простая панель
- Можно настроить за 15 минут через веб-интерфейс
- Хорошая документация на русском

### Для оптимального соотношения цена/качество:
**Timeweb Medium** (400₽/мес) ⭐
- Дешевле конкурентов
- Отличная поддержка
- Стабильная работа
- Рекомендую!

### Для корпоративного использования:
**Yandex Cloud** (~700₽/мес)
- Надежность облачного провайдера
- Возможность масштабирования
- Интеграция с другими сервисами Яндекса

---

## 🚀 Быстрый старт (Timeweb)

### Вариант "За 30 минут":

1. **Зарегистрируйтесь:** https://timeweb.com/ru/services/vds
2. **Закажите VDS Medium** (400₽/мес)
3. **Получите доступы** (придут на email)
4. **Подключитесь:** `ssh root@ваш-IP`
5. **Выполните команды:**

```bash
# Скопируйте и вставьте по очереди:

# 1. Обновление
apt update && apt upgrade -y

# 2. Установка всего необходимого
apt install -y python3 python3-pip python3-venv nginx git certbot python3-certbot-nginx

# 3. Создание пользователя
adduser docxapp
usermod -aG sudo docxapp

# 4. Переключение
su - docxapp

# 5. Загрузка кода (если на GitHub)
git clone https://github.com/ваш-username/docx-filler.git docx-template-filler
cd docx-template-filler

# 6. Установка Python окружения
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 7. Выход из пользователя docxapp
exit

# 8. Настройка автозапуска (скопируйте весь блок целиком)
cat > /etc/systemd/system/docxapp.service << 'EOF'
[Unit]
Description=DOCX Template Filler
After=network.target

[Service]
User=docxapp
Group=www-data
WorkingDirectory=/home/docxapp/docx-template-filler
Environment="PATH=/home/docxapp/docx-template-filler/venv/bin"
ExecStart=/home/docxapp/docx-template-filler/venv/bin/gunicorn --bind 127.0.0.1:8000 --workers 2 --timeout 120 app:app
Restart=always

[Install]
WantedBy=multi-user.target
EOF

# 9. Запуск сервиса
systemctl daemon-reload
systemctl enable docxapp
systemctl start docxapp

# 10. Настройка Nginx (скопируйте весь блок целиком, замените ВАШ_IP на реальный IP)
cat > /etc/nginx/sites-available/docxapp << 'EOF'
server {
    listen 80;
    server_name ВАШ_IP;
    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static {
        alias /home/docxapp/docx-template-filler/static;
    }
}
EOF

# 11. Активация Nginx
ln -s /etc/nginx/sites-available/docxapp /etc/nginx/sites-enabled/
systemctl restart nginx

# 12. Проверка
systemctl status docxapp
systemctl status nginx
```

6. **Откройте в браузере:** `http://ваш-IP-адрес`

**Готово!** Приложение работает!

---

## 📝 Загрузка файлов без GitHub

Если не хотите использовать GitHub, можно загрузить файлы напрямую:

**На вашем компьютере:**
```bash
cd /Users/m.melnikov/projects/docx-changer

# Создание архива
tar -czf docx-app.tar.gz \
  --exclude='venv' \
  --exclude='__pycache__' \
  --exclude='output/*' \
  --exclude='uploads/*' \
  .
```

**Загрузка на сервер:**
```bash
# С вашего компьютера
scp docx-app.tar.gz root@ваш-IP:/tmp/

# На сервере
su - docxapp
cd ~
tar -xzf /tmp/docx-app.tar.gz -C docx-template-filler
```

---

## 🎯 Итоговая стоимость (1 год)

### Timeweb (рекомендуется):
- VPS Medium: 400₽ × 12 = **4800₽**
- Домен .ru: **99₽**
- Автобэкапы: 50₽ × 12 = **600₽**
- **ИТОГО: 5499₽/год** (~458₽/мес)

### Beget:
- VPS-2: 690₽ × 12 = **8280₽**
- Домен .ru: **99₽**
- **ИТОГО: 8379₽/год** (~698₽/мес)

### Yandex Cloud:
- VM: 700₽ × 12 = **8400₽**
- Домен .ru: **99₽**
- Минус пробный период: **-4000₽**
- **ИТОГО: 4499₽/год** (первый год)

---

## ⚠️ Важные моменты

### Безопасность:

1. **Смените пароль root:**
```bash
passwd root
```

2. **Настройте SSH ключи** (вместо пароля)

3. **Включите firewall:**
```bash
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable
```

4. **Регулярно обновляйте:**
```bash
apt update && apt upgrade -y
```

### Резервные копии:

```bash
# Автоматические бэкапы в Timeweb: +50₽/мес
# Или ручной скрипт (см. scripts/backup.sh)
```

---

## 🆘 Поддержка

**Если что-то не работает:**

1. **Проверьте статус сервисов:**
```bash
systemctl status docxapp
systemctl status nginx
```

2. **Смотрите логи:**
```bash
journalctl -u docxapp -n 50
tail -f /var/log/nginx/error.log
```

3. **Обратитесь в поддержку хостинга** (Timeweb отвечает очень быстро)

---

## ✅ Готово!

Теперь у вас есть работающее приложение на российском хостинге с:
- ✅ Доступом по HTTP (или HTTPS с доменом)
- ✅ Автозапуском при перезагрузке
- ✅ Русской технической поддержкой
- ✅ Оплатой российскими картами

**Рекомендую:** Начните с Timeweb (400₽/мес) - лучшее соотношение цены, качества и простоты настройки для России.

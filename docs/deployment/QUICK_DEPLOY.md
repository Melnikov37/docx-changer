# 🚀 Быстрое развертывание за 10 минут

## Вариант 1: Railway (самый простой)

### Шаги:

1. **Создайте репозиторий на GitHub**
   ```bash
   cd /Users/m.melnikov/projects/docx-changer

   # Если еще не сделано:
   git init
   git add .
   git commit -m "Initial commit: DOCX Template Filler"
   ```

2. **Загрузите на GitHub**
   - Зайдите на https://github.com/new
   - Создайте репозиторий `docx-template-filler` (можно приватный)
   - Выполните команды:
   ```bash
   git remote add origin https://github.com/ВАШ-USERNAME/docx-template-filler.git
   git branch -M main
   git push -u origin main
   ```

3. **Разверните на Railway**
   - Зайдите на https://railway.app
   - Войдите через GitHub
   - New Project → Deploy from GitHub repo
   - Выберите `docx-template-filler`
   - Подождите 2-3 минуты
   - **Готово!** Railway даст вам ссылку типа `https://docx-template-filler.up.railway.app`

### Стоимость:
- **Бесплатно** первые 500 часов/месяц
- Потом **$5/месяц** (~450₽)

---

## Вариант 2: Render (тоже простой)

### Шаги:

1. **Загрузите на GitHub** (как в варианте 1)

2. **Разверните на Render**
   - Зайдите на https://render.com
   - Войдите через GitHub
   - New → Web Service
   - Подключите репозиторий `docx-template-filler`
   - Render найдет файл `render.yaml` автоматически
   - Нажмите "Create Web Service"
   - Подождите 5 минут
   - **Готово!** Получите ссылку типа `https://docx-template-filler.onrender.com`

### Стоимость:
- **Бесплатно** с ограничениями (засыпает через 15 мин)
- **$7/месяц** без засыпания (~630₽)

---

## Вариант 3: Docker локально (для тестов)

### Шаги:

1. **Установите Docker**
   - macOS: https://docs.docker.com/desktop/install/mac-install/
   - Скачайте и установите Docker Desktop

2. **Запустите приложение**
   ```bash
   cd /Users/m.melnikov/projects/docx-changer
   docker-compose up -d
   ```

3. **Откройте в браузере**
   - http://localhost:5000

4. **Остановка**
   ```bash
   docker-compose down
   ```

---

## Что выбрать?

| Ситуация | Выбор |
|----------|-------|
| Нужно быстро показать приложение | **Railway** |
| Планируется много пользователей | **Render** ($7/мес) |
| Нужен полный контроль, дешево | **VPS Hetzner** (см. DEPLOYMENT.md) |
| Только для себя/команды | **Docker локально** |

---

## После развертывания

### Проверьте работу:

1. Откройте ваш сайт
2. Загрузите шаблон из `docx_templates/example.docx`
3. Заполните форму
4. Сгенерируйте документ
5. Скачайте и проверьте результат

### Поделитесь ссылкой:

- Отправьте ссылку коллегам
- Добавьте в закладки
- Сохраните в документации

---

## Обновление приложения

### Railway/Render (автоматически):

```bash
# Внесите изменения в код
git add .
git commit -m "Update: описание изменений"
git push

# Railway/Render автоматически обновят приложение
```

### Docker:

```bash
docker-compose down
docker-compose build
docker-compose up -d
```

---

## Нужна помощь?

- Полная документация: `DEPLOYMENT.md`
- Настройка VPS: см. раздел "Вариант 3" в `DEPLOYMENT.md`
- Проблемы с развертыванием: проверьте логи в Railway/Render

**Готово!** Ваше приложение доступно в интернете! 🎉

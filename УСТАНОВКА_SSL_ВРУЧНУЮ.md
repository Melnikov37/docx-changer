# 🔐 Установка SSL сертификата вручную

Для установки SSL сертификата от Timeweb или любого другого провайдера.

---

## 📥 Шаг 1: Получите файлы сертификата

### В панели Timeweb:

1. Откройте: https://timeweb.cloud
2. Перейдите в **Услуги** → **SSL-сертификаты**
3. Выберите ваш сертификат для `docxfiller.ru`
4. Нажмите **"Скачать"**

Вы получите файлы:
- ✅ `private.key` - приватный ключ
- ✅ `certificate.crt` - сертификат
- ✅ `ca_bundle.crt` - цепочка сертификатов (опционально)

⚠️ **ВАЖНО:** Никогда не публикуйте файл `private.key`! Это секретный ключ.

---

## 📤 Шаг 2: Загрузите файлы на сервер

На **вашем Mac**, из папки где лежат скачанные файлы:

```bash
# Загрузите файлы на сервер
scp private.key root@85.239.39.232:/tmp/
scp certificate.crt root@85.239.39.232:/tmp/
scp ca_bundle.crt root@85.239.39.232:/tmp/   # если есть
```

**Примечание:** Файлы загружаются во временную папку `/tmp/` для безопасности.

---

## 🚀 Шаг 3: Запустите скрипт установки

Подключитесь к серверу и запустите автоматический скрипт:

```bash
# Подключение к серверу
ssh root@85.239.39.232

# Скачайте скрипт из GitHub
curl -O https://raw.githubusercontent.com/Melnikov37/docx-changer/main/scripts/install_ssl_manual.sh

# Запустите установку
bash install_ssl_manual.sh
```

**Скрипт спросит:**
- Ваш домен (например: `docxfiller.ru`)

**Скрипт автоматически:**
1. ✅ Проверит наличие файлов сертификата
2. ✅ Создаст директорию `/etc/nginx/ssl/`
3. ✅ Скопирует сертификаты с правильными правами
4. ✅ Создаст конфигурацию Nginx для HTTPS
5. ✅ Настроит редирект HTTP → HTTPS
6. ✅ Добавит заголовки безопасности
7. ✅ Проверит конфигурацию
8. ✅ Перезапустит Nginx
9. ✅ Удалит временные файлы
10. ✅ Проверит работу HTTPS

**Время выполнения:** ~30 секунд

---

## ✅ Шаг 4: Проверка

Откройте в браузере: **https://docxfiller.ru**

Должен появиться **зеленый замочок** 🔒 в адресной строке!

---

## 🔄 Что делает скрипт

### Файловая структура после установки:

```
/etc/nginx/ssl/
├── docxfiller.ru.key              # Приватный ключ (права: 600)
├── docxfiller.ru.crt              # Сертификат (права: 644)
└── docxfiller.ru-fullchain.crt    # Полная цепочка (если был ca_bundle)
```

### Конфигурация Nginx:

```nginx
# HTTP → HTTPS редирект
server {
    listen 80;
    server_name docxfiller.ru www.docxfiller.ru;
    return 301 https://$host$request_uri;
}

# HTTPS сервер
server {
    listen 443 ssl http2;
    server_name docxfiller.ru www.docxfiller.ru;

    ssl_certificate /etc/nginx/ssl/docxfiller.ru-fullchain.crt;
    ssl_certificate_key /etc/nginx/ssl/docxfiller.ru.key;

    # ... остальная конфигурация
}
```

---

## 🔍 Проверка SSL

### Проверка через curl:

```bash
# Проверка HTTP редиректа
curl -I http://docxfiller.ru
# Должен вернуть: 301 Moved Permanently

# Проверка HTTPS
curl -I https://docxfiller.ru
# Должен вернуть: 200 OK
```

### Проверка сертификата:

```bash
# Информация о сертификате
openssl s_client -connect docxfiller.ru:443 -servername docxfiller.ru

# Срок действия
echo | openssl s_client -connect docxfiller.ru:443 -servername docxfiller.ru 2>/dev/null | openssl x509 -noout -dates
```

### Онлайн проверка:

- **SSL Labs:** https://www.ssllabs.com/ssltest/analyze.html?d=docxfiller.ru
- **Качество SSL:** покажет рейтинг безопасности (должен быть A или выше)

---

## ⚠️ Если что-то пошло не так

### Ошибка: "Файлы не найдены"

**Проблема:** Файлы не загружены в `/tmp/`

**Решение:**
```bash
# Проверьте что файлы загружены
ls -la /tmp/*.{key,crt}

# Если нет - загрузите снова с вашего Mac
scp private.key root@85.239.39.232:/tmp/
scp certificate.crt root@85.239.39.232:/tmp/
```

### Ошибка: "nginx: configuration test failed"

**Проблема:** Ошибка в конфигурации Nginx

**Решение:**
```bash
# Посмотрите что не так
nginx -t

# Проверьте конфигурацию
cat /etc/nginx/sites-available/docxapp

# Проверьте что файлы сертификата существуют
ls -la /etc/nginx/ssl/
```

### Браузер показывает "небезопасное соединение"

**Возможные причины:**

1. **Сертификат для другого домена**
   - Проверьте что сертификат выпущен для `docxfiller.ru`

2. **Неполная цепочка сертификатов**
   - Убедитесь что загрузили `ca_bundle.crt`
   - Используйте fullchain: `ssl_certificate /etc/nginx/ssl/docxfiller.ru-fullchain.crt;`

3. **Сертификат истёк**
   - Проверьте срок действия: `openssl x509 -in /etc/nginx/ssl/docxfiller.ru.crt -noout -dates`

### HTTPS не работает, только HTTP

**Проблема:** Порт 443 закрыт в firewall

**Решение:**
1. Откройте панель Timeweb: https://timeweb.cloud
2. Выберите ваш VPS
3. Вкладка **"Firewall"**
4. Добавьте правило:
   - Порт: **443**
   - Протокол: **TCP**
   - Источник: **Любой** (0.0.0.0/0)

---

## 🔄 Обновление сертификата

Сертификаты Timeweb обычно действуют **1 год**.

За ~30 дней до истечения:

1. Обновите сертификат в панели Timeweb
2. Скачайте новые файлы
3. Запустите скрипт установки снова:

```bash
# Загрузите новые файлы
scp private.key root@85.239.39.232:/tmp/
scp certificate.crt root@85.239.39.232:/tmp/
scp ca_bundle.crt root@85.239.39.232:/tmp/

# Запустите установку
ssh root@85.239.39.232
bash install_ssl_manual.sh
```

---

## 🛡️ Безопасность

### Что делает скрипт для безопасности:

1. ✅ Устанавливает права `600` на приватный ключ (только root может читать)
2. ✅ Удаляет временные файлы из `/tmp/` после установки
3. ✅ Добавляет заголовки безопасности (HSTS, X-Frame-Options)
4. ✅ Использует только современные протоколы (TLS 1.2, 1.3)
5. ✅ Настраивает безопасные шифры

### Где хранятся файлы:

```
/etc/nginx/ssl/docxfiller.ru.key       # Только root может читать (600)
/etc/nginx/ssl/docxfiller.ru.crt       # Публичный файл (644)
/etc/nginx/ssl/docxfiller.ru-fullchain.crt  # Публичный файл (644)
```

---

## 📝 Полезные команды

```bash
# Статус Nginx
systemctl status nginx

# Перезапуск Nginx
systemctl reload nginx

# Проверка конфигурации
nginx -t

# Логи Nginx
tail -f /var/log/nginx/error.log
tail -f /var/log/nginx/access.log

# Проверка SSL
openssl s_client -connect docxfiller.ru:443 -servername docxfiller.ru

# Список сертификатов
ls -la /etc/nginx/ssl/
```

---

## ✅ Итоговый чек-лист

- [ ] Скачал файлы сертификата из Timeweb
- [ ] Загрузил файлы на сервер в `/tmp/`
- [ ] Запустил скрипт `install_ssl_manual.sh`
- [ ] Скрипт завершился успешно
- [ ] Открыл `https://docxfiller.ru` в браузере
- [ ] Вижу зеленый замочок 🔒
- [ ] HTTP редиректит на HTTPS
- [ ] Временные файлы удалены

**Готово!** Ваш сайт работает по HTTPS! 🎉

# 🌐 Настройка домена и SSL сертификата

## Шаг 1: Покупка домена (если еще нет)

### Российские регистраторы:

| Регистратор | Цена | Оплата | Ссылка |
|-------------|------|--------|--------|
| **REG.RU** | от 99₽/год | МИР, российские карты, СБП | https://www.reg.ru/domain/new/ |
| **2domains.ru** | от 149₽/год | МИР, российские карты | https://2domains.ru |
| **Timeweb** | от 199₽/год | МИР, российские карты, СБП | https://timeweb.com/ru/services/domains |

**Рекомендую:** REG.RU (самый дешевый и надежный)

---

## Шаг 2: Настройка DNS записей

После покупки домена зайдите в **панель управления DNS** вашего регистратора:

### Добавьте A-запись для основного домена:

```
Тип: A
Имя: @ (или оставьте пустым)
Значение: 85.239.39.232
TTL: 3600
```

### Добавьте A-запись для www (опционально):

```
Тип: A
Имя: www
Значение: 85.239.39.232
TTL: 3600
```

### Где найти настройки DNS:

**REG.RU:**
1. Зайдите в личный кабинет
2. Выберите ваш домен
3. Вкладка "Управление зонами DNS"
4. Нажмите "Добавить запись"

**Timeweb:**
1. Панель управления → Домены
2. Выберите домен → DNS
3. Добавить запись

**2domains.ru:**
1. Мои домены → Выбрать домен
2. DNS → Добавить запись

⏱️ **Время обновления DNS:** 5-30 минут (иногда до 24 часов)

---

## Шаг 3: Проверка DNS

Перед установкой SSL проверьте что DNS обновился.

**На вашем компьютере:**

```bash
# Для Mac/Linux:
dig +short ваш-домен.ru

# Для Windows (PowerShell):
nslookup ваш-домен.ru

# Должно вернуть: 85.239.39.232
```

**Или онлайн:**
- https://dnschecker.org
- Введите ваш домен
- Проверьте что A-запись указывает на 85.239.39.232

---

## Шаг 4: Установка SSL сертификата (Let's Encrypt)

### Автоматический способ (рекомендуется):

Подключитесь к серверу и запустите скрипт:

```bash
ssh root@85.239.39.232

# Скачайте скрипт из репозитория
curl -O https://raw.githubusercontent.com/Melnikov37/docx-changer/main/scripts/setup_domain_ssl.sh

# Запустите
bash setup_domain_ssl.sh
```

**Скрипт спросит:**
1. Ваш домен (например: `example.com`)
2. Email для уведомлений Let's Encrypt

**Скрипт автоматически:**
- ✅ Проверит DNS
- ✅ Обновит конфигурацию Nginx
- ✅ Установит SSL сертификат
- ✅ Настроит автоматическое обновление

**Время выполнения:** 1-2 минуты

---

### Ручной способ:

Если хотите настроить вручную:

```bash
ssh root@85.239.39.232

# 1. Обновите конфигурацию Nginx
nano /etc/nginx/sites-available/docxapp

# Замените строку:
#   server_name _;
# На:
#   server_name ваш-домен.ru www.ваш-домен.ru;

# Сохраните: Ctrl+O, Enter, Ctrl+X

# 2. Проверьте конфигурацию
nginx -t

# 3. Перезапустите Nginx
systemctl reload nginx

# 4. Установите SSL сертификат
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Certbot спросит:
# - Email: введите ваш email
# - Согласны с условиями: Y
# - Получать новости: N (или Y, как хотите)
# - Редирект HTTP → HTTPS: выберите 2 (Yes)
```

---

## Шаг 5: Проверка работы

После установки откройте в браузере:

**https://ваш-домен.ru**

Должно открыться ваше приложение с **зеленым замочком** 🔒 в адресной строке!

---

## Автоматическое обновление SSL

Сертификат Let's Encrypt действует **90 дней**.

Certbot автоматически обновляет сертификат через systemd timer:

```bash
# Проверка статуса автообновления
systemctl status certbot.timer

# Тестовое обновление (dry-run)
certbot renew --dry-run
```

---

## Если что-то пошло не так

### DNS не обновляется

**Проблема:** `dig +short ваш-домен.ru` не возвращает 85.239.39.232

**Решение:**
- Подождите 30-60 минут
- Проверьте правильность A-записи в панели регистратора
- Убедитесь что A-запись активна (не на паузе)

### Certbot выдает ошибку "Connection refused"

**Проблема:** Порты 80 или 443 закрыты

**Решение:**
```bash
# Проверьте firewall в панели Timeweb
# Должны быть открыты порты:
# - 80 (HTTP)
# - 443 (HTTPS)
```

### Certbot выдает ошибку "DNS resolution failed"

**Проблема:** DNS еще не обновился

**Решение:**
- Подождите еще 30-60 минут
- Запустите скрипт снова

### Сайт открывается но без SSL

**Проблема:** Certbot не установил редирект

**Решение:**
```bash
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru --redirect
```

---

## Стоимость

| Услуга | Цена | Частота |
|--------|------|---------|
| Домен .ru | 99-300₽ | Ежегодно |
| SSL сертификат | **Бесплатно** (Let's Encrypt) | Обновляется автоматически |
| VPS Timeweb | 400₽ | Ежемесячно |
| **Итого** | ~500₽/мес | - |

---

## После настройки

Ваше приложение будет доступно по адресу:

- **https://ваш-домен.ru** ✅ Основной
- **https://www.ваш-домен.ru** ✅ С www
- **http://ваш-домен.ru** → автоматический редирект на HTTPS
- **http://85.239.39.232** ✅ По IP (без SSL)

---

## Полезные команды

```bash
# Статус SSL сертификата
certbot certificates

# Принудительное обновление
certbot renew --force-renewal

# Удаление сертификата
certbot delete --cert-name ваш-домен.ru

# Проверка конфигурации Nginx
nginx -t

# Перезапуск Nginx
systemctl reload nginx
```

---

**Готово!** Теперь ваше приложение работает по HTTPS с зеленым замочком! 🔒✨
